# name: test/sql/trie_basic.test
# description: basic smoke tests for suffix-trie builder only
# group: [sql]

require splink_udfs

# Build a trie from two addresses; result should be a BLOB
query I
WITH addrs(id, tokens) AS (
  SELECT 1, ['10','DOWNING','STREET','LONDON']
  UNION ALL
  SELECT 2, ['40','AVERILL','STREET','LONDON']
)
SELECT typeof(build_suffix_trie(id, tokens))
FROM addrs;
----
BLOB
